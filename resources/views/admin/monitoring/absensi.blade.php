@extends('layouts.app-dashboard')
@section('title', 'Monitoring Absensi')
@section('nav-links')
<a href="{{ route('admin.dashboard') }}" class="px-4 py-2 rounded-xl text-sm font-semibold text-gray-600 hover:text-blue-600 hover:bg-blue-50 transition-all">Dashboard</a>
<a href="{{ route('admin.monitoring.absensi') }}" class="px-4 py-2 rounded-xl text-sm font-bold bg-gradient-to-r from-blue-500 to-indigo-600 text-white shadow-md hover:shadow-lg transition-all">Absensi</a>
<a href="{{ route('admin.monitoring.laporan') }}" class="px-4 py-2 rounded-xl text-sm font-semibold text-gray-600 hover:text-blue-600 hover:bg-blue-50 transition-all">Laporan</a>
@endsection
@section('mobile-nav-links')
<a href="{{ route('admin.dashboard') }}" class="block px-4 py-3 rounded-xl text-base font-semibold text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all mb-2">Dashboard</a>
<a href="{{ route('admin.monitoring.absensi') }}" class="block px-4 py-3 rounded-xl text-base font-bold bg-gradient-to-r from-blue-500 to-indigo-600 text-white mb-2">Absensi</a>
<a href="{{ route('admin.monitoring.laporan') }}" class="block px-4 py-3 rounded-xl text-base font-semibold text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all mb-2">Laporan</a>
@endsection
@section('content')
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
<div class="mb-8"><h1 class="text-3xl md:text-4xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">📊 Monitoring Absensi</h1><p class="mt-2 text-gray-600">Pantau kehadiran cleaning service real-time</p></div>
<div class="bg-white rounded-2xl shadow-lg border border-gray-100 p-6 mb-6">
<div class="flex items-center space-x-3 mb-4"><div class="w-9 h-9 bg-gradient-to-br from-indigo-100 to-purple-100 rounded-xl flex items-center justify-center"><svg class="w-5 h-5 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path></svg></div><h3 class="text-base font-bold text-gray-900">🔍 Filter Data</h3></div>
<form method="GET" action="{{ route('admin.monitoring.absensi') }}" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
<div><label for="tanggal" class="block text-xs font-bold text-gray-600 mb-2 uppercase tracking-wider">📅 Tanggal</label><input type="date" name="tanggal" id="tanggal" value="{{ request('tanggal', now()->format('Y-m-d')) }}" class="w-full px-4 py-2.5 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"></div>
<div><label for="cs_profile_id" class="block text-xs font-bold text-gray-600 mb-2 uppercase tracking-wider">👤 CS</label><select name="cs_profile_id" id="cs_profile_id" class="w-full px-4 py-2.5 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"><option value="">Semua CS</option>@foreach($csProfiles as $cs)<option value="{{ $cs->id }}" {{ request('cs_profile_id') == $cs->id ? 'selected' : '' }}>{{ $cs->user->name }}</option>@endforeach</select></div>
<div><label for="area_id" class="block text-xs font-bold text-gray-600 mb-2 uppercase tracking-wider">📍 Area</label><select name="area_id" id="area_id" class="w-full px-4 py-2.5 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"><option value="">Semua Area</option>@foreach($areas as $area)<option value="{{ $area->id }}" {{ request('area_id') == $area->id ? 'selected' : '' }}>{{ $area->nama_area }}</option>@endforeach</select></div>
<div><label for="status" class="block text-xs font-bold text-gray-600 mb-2 uppercase tracking-wider">✅ Status</label><select name="status" id="status" class="w-full px-4 py-2.5 bg-gray-50 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition"><option value="">Semua Status</option><option value="hadir" {{ request('status') == 'hadir' ? 'selected' : '' }}>Hadir</option><option value="izin" {{ request('status') == 'izin' ? 'selected' : '' }}>Izin</option><option value="sakit" {{ request('status') == 'sakit' ? 'selected' : '' }}>Sakit</option></select></div>
<div class="flex items-end"><button type="submit" class="w-full px-6 py-2.5 bg-gradient-to-r from-indigo-500 to-purple-500 text-white rounded-xl font-bold hover:from-indigo-600 hover:to-purple-600 transition shadow-lg hover:shadow-xl">Filter</button></div>
</form>
</div>
<div class="lg:hidden space-y-4 mb-6">
@forelse($absensis as $absensi)
<div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
<div class="bg-gradient-to-r {{ $absensi->status === 'hadir' ? 'from-green-500 to-teal-500' : ($absensi->status === 'izin' ? 'from-yellow-500 to-orange-500' : 'from-red-500 to-pink-500') }} px-4 py-3">
<div class="flex items-center justify-between"><div class="flex items-center space-x-3 flex-1 min-w-0"><img src="{{ asset('storage/' . $absensi->foto_wajah) }}" alt="Foto" class="w-12 h-12 rounded-full border-2 border-white shadow-lg cursor-pointer object-cover" onclick="openModal('{{ asset('storage/' . $absensi->foto_wajah) }}')"><div class="flex-1 min-w-0"><p class="text-white font-bold text-sm truncate">{{ $absensi->csProfile->user->name }}</p><p class="text-white/80 text-xs truncate">{{ $absensi->tanggal->format('d M Y') }}</p></div></div><span class="px-3 py-1 bg-white/20 backdrop-blur-sm text-white text-xs font-bold rounded-full">{{ $absensi->jam_absen }}</span></div>
</div>
<div class="p-4 space-y-3"><div class="flex items-center justify-between"><span class="px-3 py-1 bg-blue-100 text-blue-800 text-xs font-bold rounded-full">📍 {{ $absensi->area->nama_area }}</span>@if($absensi->status === 'hadir')<span class="px-3 py-1 bg-green-100 text-green-800 text-xs font-bold rounded-full">✓ Hadir</span>@elseif($absensi->status === 'izin')<span class="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs font-bold rounded-full">⏸ Izin</span>@else<span class="px-3 py-1 bg-red-100 text-red-800 text-xs font-bold rounded-full">✗ Sakit</span>@endif</div>@if($absensi->keterangan)<div class="p-3 bg-gray-50 rounded-xl border border-gray-200"><p class="text-xs text-gray-700">💬 {{ $absensi->keterangan }}</p></div>@endif</div>
</div>
@empty
<div class="bg-white rounded-2xl shadow-lg p-12 text-center"><div class="w-20 h-20 mx-auto mb-4 bg-gray-100 rounded-full flex items-center justify-center"><svg class="h-10 w-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg></div><p class="font-bold text-gray-700">Tidak ada data</p></div>
@endforelse
</div>
<div class="hidden lg:block bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
<table class="min-w-full divide-y divide-gray-200"><thead><tr class="bg-gradient-to-r from-indigo-500 to-purple-600"><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">No</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Foto</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Nama CS</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Tanggal</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Jam</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Area</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Status</th><th class="px-4 py-4 text-left text-xs font-bold text-white uppercase">Keterangan</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($absensis as $index => $absensi)<tr class="hover:bg-gradient-to-r hover:from-indigo-50 hover:to-purple-50 transition"><td class="px-4 py-4"><div class="w-8 h-8 bg-gradient-to-br from-indigo-400 to-purple-500 rounded-full flex items-center justify-center"><span class="text-white font-bold text-xs">{{ $absensis->firstItem() + $index }}</span></div></td><td class="px-4 py-4"><img src="{{ asset('storage/' . $absensi->foto_wajah) }}" alt="Foto" class="w-12 h-12 rounded-full object-cover cursor-pointer hover:ring-4 ring-indigo-200 transition" onclick="openModal('{{ asset('storage/' . $absensi->foto_wajah) }}')"></td><td class="px-4 py-4"><div class="text-sm font-bold text-gray-900">{{ $absensi->csProfile->user->name }}</div><div class="text-xs text-gray-500">{{ $absensi->csProfile->user->email }}</div></td><td class="px-4 py-4 text-sm font-semibold text-gray-900">{{ $absensi->tanggal->format('d M Y') }}</td><td class="px-4 py-4 text-sm font-black text-indigo-600">{{ $absensi->jam_absen }}</td><td class="px-4 py-4"><span class="px-3 py-1 bg-blue-100 text-blue-800 text-xs font-bold rounded-full">{{ $absensi->area->nama_area }}</span></td><td class="px-4 py-4">@if($absensi->status === 'hadir')<span class="px-3 py-1 bg-green-100 text-green-800 text-xs font-bold rounded-full">✓ Hadir</span>@elseif($absensi->status === 'izin')<span class="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs font-bold rounded-full">⏸ Izin</span>@else<span class="px-3 py-1 bg-red-100 text-red-800 text-xs font-bold rounded-full">✗ Sakit</span>@endif</td><td class="px-4 py-4 text-sm text-gray-500 max-w-xs truncate">{{ $absensi->keterangan ?? '-' }}</td></tr>@empty<tr><td colspan="8" class="px-6 py-12 text-center"><div class="w-20 h-20 mx-auto mb-4 bg-gray-100 rounded-full flex items-center justify-center"><svg class="h-10 w-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg></div><p class="font-bold text-gray-700">Tidak ada data</p></td></tr>@endforelse</tbody></table>
@if($absensis->hasPages())<div class="px-6 py-4 border-t border-gray-200 bg-gray-50">{{ $absensis->links() }}</div>@endif
</div>
@if($absensis->hasPages())<div class="lg:hidden mt-4">{{ $absensis->links() }}</div>@endif
</div>
<div id="imageModal" class="hidden fixed inset-0 bg-black/75 backdrop-blur-sm z-50 flex items-center justify-center p-4" onclick="closeModal()"><div class="relative max-w-3xl w-full"><button onclick="closeModal()" class="absolute -top-12 right-0 text-white bg-white/10 hover:bg-white/20 rounded-full p-2 transition"><svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg></button><img id="modalImage" src="" alt="Zoom" class="w-full h-auto rounded-2xl shadow-2xl"></div></div>
<script>function openModal(src){document.getElementById('modalImage').src=src;document.getElementById('imageModal').classList.remove('hidden')}function closeModal(){document.getElementById('imageModal').classList.add('hidden')}document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()})</script>
@endsection
